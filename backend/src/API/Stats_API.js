import { sample_matches } from "../data";
import { MatchModel } from "../models/match.model";

const match = sample_matches;


calculateStats(sample_matches) {

}

legWins(id,match){

    for (let i = 0; i < match.length; i++ ){

    }
}
